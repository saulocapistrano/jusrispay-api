services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: jurispay
      POSTGRES_USER: jurispay
      POSTGRES_PASSWORD: jurispay
    ports:
      - "15432:5432"
    volumes:
      - jurispay_pgdata:/var/lib/postgresql/data
    networks:
      - jurispay_backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jurispay -d jurispay"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:latest
    ports:
      - "19000:9000"   # API S3
      - "19001:9001"   # Console web
    environment:
      MINIO_ROOT_USER: jurispay
      MINIO_ROOT_PASSWORD: jurispay123
    command: server /data --console-address ":9001"
    volumes:
      - jurispay_minio_data:/data
    networks:
      - jurispay_backend
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:9000/minio/health/ready > /dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - jurispay_backend
    entrypoint: >
      /bin/sh -c "
      mc alias set jurispay http://minio:9000 jurispay jurispay123;
      mc mb -p jurispay/jurispay-documents || true;
      mc anonymous set none jurispay/jurispay-documents || true;
      echo 'MinIO bucket ready';
      "

  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "18080:8080"
    networks:
      - jurispay_backend
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy

networks:
  jurispay_backend:
    driver: bridge

volumes:
  jurispay_pgdata:
  jurispay_minio_data:

