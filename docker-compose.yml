version: '3.8'

services:
  db:
    image: postgres:16
    container_name: jurispay-db
    environment:
      POSTGRES_DB: jurispay
      POSTGRES_USER: jurispay
      POSTGRES_PASSWORD: jurispay
    ports:
      - "15432:5432"
    volumes:
      - jurispay_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jurispay -d jurispay"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:latest
    container_name: jurispay-minio
    ports:
      - "19000:9000"   # API S3
      - "19001:9001"   # Console web
    environment:
      MINIO_ROOT_USER: jurispay
      MINIO_ROOT_PASSWORD: jurispay123
    command: server /data --console-address ":9001"
    volumes:
      - jurispay_minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "mc ready local || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio-init:
    image: minio/mc:latest
    container_name: jurispay-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set jurispay http://minio:9000 jurispay jurispay123;
      mc mb -p jurispay/jurispay-documents || true;
      mc anonymous set none jurispay/jurispay-documents || true;
      echo 'MinIO bucket ready';
      "

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jurispay-api
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "18080:8080"
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy

volumes:
  jurispay_pgdata:
  jurispay_minio_data:

